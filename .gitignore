# Stale: the PlatformIO build is gone, so this can be deleted outright.
.pio/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

firmware.map

# CMake host build. Failure pictures land in build/tests/artifacts/ -- see
# tests/README.md. They are regenerated by every ctest run, so they are never
# committed.
build/
build-teensy41/
build-stm32u5g9j/
targets/*/build/
statex-test-artifacts/

# Python bytecode from tools/
__pycache__/
*.pyc
